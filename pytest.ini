[pytest]
addopts = -p no:warnings
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    update_conversation: tests for conversation update routes
    sub_conversation: tests for sub-conversation creation routes
